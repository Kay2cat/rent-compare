# 🚀 升級說明 — 首頁整併版

這次的三項改動，重點是**把「投票」整併進首頁表格**，並修好兩個解析問題。

## 這次改了什麼

| 改動 | 說明 | 影響檔案 |
|:---|:---|:---|
| 🔧 補回遺失檔案 | 上次的 `js/commute.js`、`js/quick-add.js` 沒被加進 git，導致線上版 JS 載入失敗。這次補回 | `js/commute.js`、`js/quick-add.js`（新增） |
| 📐 格局抓「衛」 | 快速新增解析改為分別抓房／廳／衛，例：`3房1廳2衛`；也支援套房、開放式 | `js/quick-add.js` |
| 🏷 解析「其他特色」 | 591 的「其他特色」（近捷運、可養寵物、可開伙…）自動解析成標籤，寫入 K 欄並與既有標籤去重合併 | `js/quick-add.js`、`index.html`、`Code.gs` |
| 🗳 投票整併進首頁 | 移除獨立的「投票」頁籤。改為**點表格任一列就地展開**，展開區有圖片、完整標籤、打星星、留言框、所有室友的留言串 | `js/table-view.js`、`js/vote-panel.js`、`index.html`、`css/index.css` |

## 點列展開長怎樣

- 表格每列最前面有個 ▸ 箭頭，點該列任意處（除了連結、刪除鈕、狀態選單）就會在下方展開
- 一次只展開一列，點別列會自動收前一個，再點同一列收合
- 展開區左邊是「你的評分 + 留言框 + 送出鈕」，右邊是「所有室友的留言串」
- 投票欄平常顯示平均分、投票者圓點、留言數（💬 N），提示「點此投票/留言」
- 手機上展開區自動改為上下堆疊

## 升級步驟

### 1. 替換前端
把整個資料夾覆蓋你的 repo 後 push。**這次特別注意**：`git add -A` 確保 `js/commute.js` 和 `js/quick-add.js` 兩個檔案有被加進去（用 `git status` 確認沒有 untracked 檔案）。

### 2. 更新 Apps Script ⚠️ 必做
新版 `Code.gs` 多了標籤合併邏輯（`_mergeTags`）與 features 支援。貼上後記得：
**部署 → 管理部署作業 → 編輯（鉛筆）→ 版本選「新版本」→ 部署**（網址不變）。

### 3. Google Sheet 無需改動
這次沒有新增欄位。「其他特色」會寫進既有的 K 欄（優缺點標籤）。

## 小提醒
- 你目前線上的網站因為缺那兩個檔案，新增物件和通勤功能應該是壞的——這次補上後就會恢復
- 「其他特色」解析出的詞若要有專屬顏色，可以到「標籤定義」工作表補上對應的分類與顏色，否則會用預設灰色顯示
